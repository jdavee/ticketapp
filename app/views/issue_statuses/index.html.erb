<div class="main-heading">
<span><%= link_to 'Create New Issue Status', new_issue_status_path, :class => "heading-btn btn btn-primary btn-sm" %></span>
<h3>All Issue Statuses</h3>
</div>

<table class="table table-striped table-hover table-responsive">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @issue_statuses.each do |issue_status| %>
      <tr>
        <td><%= issue_status.name %></td>
        <td class="list_actions">
          <div class="btn-group">
            <%= link_to "Edit", edit_issue_status_path(issue_status), :class => "btn btn-default btn-xs" %>
            <%= link_to "Delete", issue_status, method: :delete, data: {confirm: 'Are you sure?' }, :class => "btn btn-primary btn-xs" %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<div class="main-heading"></div>
<br>
