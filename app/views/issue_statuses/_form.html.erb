<%= form_for(@issue_status) do |f| %>
  <% if @issue_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue_status.errors.count, "error") %> prohibited this issue_status from being saved:</h2>

      <ul>
      <% @issue_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field input-group form-center">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class => 'form-control' %>
  </div>
  <div class="actions">
    <%= submit_tag "Submit Issue Status", :class => 'btn btn-primary blue-btn' %>
    <%= link_to 'Cancel',  issue_statuses_path, :class => 'btn btn-primary blue-btn' %>
  </div>
<% end %>
